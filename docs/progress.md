# Fairtoon 프로젝트 진행 기록 (progress.md)


날짜별 누적 히스토리. "무엇을 했는가"의 기록. 현재 스냅샷·다음 액션은 `handover.md` 참조.

> **이전 이력 아카이브**
> - `backup/progress_2026-08-28-0055.md` (2026-08-16 ~ 08-21, 414줄) — 권한 거부 목록 도입, Edge Function 배포, 동기화 자동화 가동, 카카오페이지 완결작 3,087편 수집으로 카탈로그 7,602편.
> - `backup/progress_2026-08-16-1442.md` (2026-08-02 ~ 08-15, 335줄) — P4 본격 진행. 카탈로그 50편 → 3,017편, 인기 순위·작품 찾아보기 화면 신설.
> - `backup/progress_2026-08-03-0053.md` (2026-06-17 ~ 08-02, 391줄) — 착수부터 P0~P3 완료·P4 착수까지.


## 이어지는 맥락 (carry-over · 2026-08-31 기준)

- **카탈로그 12,859편** (완결 9,253 · 연재 3,606). 국내 5대 플랫폼이 모두 채워졌다 — 레진 4,980 · 카카오페이지 4,288 · 네이버 3,120 · 리디 412 · 카카오웹툰 203.
- **실행 대기 SQL이 하나도 없다.** 23번부터 **30번까지** 전부 실행·검증 완료.
- **자동화 3건 정상.** 인기 순위 07:37 · 연재작 동기화 07:57 · keepalive 12:17. 08-27에 정각 회피로 시각을 옮겼다.
- **아티클 6편이 전부 열린다.** 08-28에 쓴 3·6번 본문을 대표님이 08-31에 30번 SQL로 투입하셨고, 같은 날 `index.html`을 푸시해 라이브에 반영했다. 화면에서 "준비 중"이 사라졌다.
- **미커밋 변경 없음.** 다만 리포에 아직 안 올린 신규 폴더가 있다 — `Renew/` · `project-notes/` · `etc/`(녹취·스크린샷) · `output/` 시안 폴더들. 디자인 리뉴얼 쪽 작업물이라 이번 푸시에서는 제외했다.
- **대기 — 제휴 문의 발송**(문안 완성, 발송 명의·회신 이메일 미정) · **지호님 작가명 검수 7건** · '지금 인기' 상단 메뉴 편입 여부.


## 2026-08-27 — 레진·리디 투입으로 카탈로그 12,859편 (5대 플랫폼 완비)

08-22에 만들어 두고 실행하지 않은 SQL 18개가 남아 있던 것을 발견해 처리한 날이다. 대표님이 "해야 할 일을 한번 정리해 달라"고 하신 데서 시작해, 그날 안에 투입·검증·정리까지 끝냈다.


### 1. 착수 시점의 발견 — 기록과 실제가 어긋나 있었다

handover.md는 08-21자였고 카탈로그 7,602편으로 적혀 있었다. 그런데 리포에는 08-22에 만든 레진 16배치·리디 2배치 SQL과 수집 스크립트 2개가 커밋도 안 된 채 놓여 있었다. **문서만 읽고 보고했다면 없는 상태를 보고할 뻔했다.**

그래서 DB·워크플로·리포를 직접 조회해 현황을 다시 세웠다.

| 항목 | 문서 기준 | 실제 |
|---|---|---|
| 카탈로그 | 7,602편 | 7,613편 |
| 레진·리디 | 언급 없음 | SQL 18개(5,392편) 미실행 |
| 아티클 | 3·6번 본문 없음 | 그대로 (0자) |
| git | 최신 커밋 반영 | 원격보다 5커밋 뒤 |

**교훈 — handover.md는 스냅샷이지 진실이 아니다. 보고 전에 DB를 찍어 본다.**


### 2. 투입 순서 판단 — 로드 경량화가 필요한지 먼저 실측했다

5,392편을 넣으면 전송량이 5.08MB가 된다. 08-19에 45% 경량화하기 전 수준(5.55MB)에 근접한다. 그래서 A안(경량화 먼저)·B안(투입 먼저)을 대표님께 올렸다.

그런데 실측해 보니 **판단의 전제가 틀려 있었다.** 브라우저는 압축본을 받는다.

| 구분 | 원본 | 압축 실전송 |
|---|---|---|
| works 13페이지 | 5.08MB | **0.98MB** |
| 표지 포함 첫 화면 총량 | — | **2.81MB** (7,602편 시절 2.97MB보다 오히려 적다) |

편수가 69% 늘었는데 통신량은 줄었다. **로드 2차 개선은 불필요하다고 판단해 대표님께 그대로 보고했다.** 지난 구간의 "2.97MB"도 압축 전 값이었음을 함께 정정했다.


### 3. 투입 결과 — 결함 0

대표님이 25·26번 SQL 18개를 실행했다. 12,864편 전부를 대상으로 확인했다.

| 항목 | 결과 |
|---|---|
| 작품 | 7,613 → **12,864편** (+5,251) |
| 플랫폼 | 레진 **4,980**(신규) · 카카오페이지 4,288 · 네이버 3,125 · 리디 **268**(신규) · 카카오웹툰 203 |
| 링크 없는 작품 | 0편 |
| 작가·장르 결측 | 0편 |
| 제목 중복 | 0건 |
| 신규 표본 20편 실제 접속 | 링크·표지 **20/20 정상** |
| 라이브 12회 반복 로드 | 첫 화면 2.6~3.8초 · JS 오류 0 · 12,864편 전량 수신 |
| 검색 실측 | '레바툰' → ⓒ 레진코믹스 · '쌍룡여의' → ⓒ 리디 |

**리디가 412편이 아니라 268편만 들어갔다.** 차액 144편은 레진에도 같은 제목이 있어 "제목이 이미 있으면 넣지 않는다"는 규칙대로 걸러진 것이다. SQL의 제목을 파싱해 세어 보니 리디∩레진 교집합이 정확히 144건이어서 수치가 맞아떨어졌다. 누락이 아니라 설계대로 동작한 결과다.


### 4. 리디 링크 144편 보강 — 데이터만 넣으면 화면에 안 보였다

144편에 리디 링크를 붙이는 SQL(27번)을 만들다가, `adoptWorks()`가 **작품당 첫 번째 링크만 쓰고 있음**을 발견했다. 링크를 넣어도 화면에는 아무 변화가 없었을 것이다.

그래서 화면도 함께 고쳤다 — **검색 결과가 한 편일 때**, 그 작품의 정식 링크가 있는 플랫폼 카드를 검색이 아니라 작품 페이지로 보낸다. 여러 편이 걸렸을 때는 어느 작품의 링크인지 알 수 없어 쓰지 않는다.

라이브 검증 결과:

| 검색어 | 결과 |
|---|---|
| 먹는 존재 · 환관제조일기 (2링크 보유) | 리디·레진 **둘 다 "작품 페이지 ↗"** |
| 레바툰 | 레진만 작품 페이지, 나머지 4곳은 검색 |
| 미생 (7편 매칭) | 전부 검색 유지 |

**데이터를 넣기 전에 그 데이터가 화면에 닿는 경로를 확인한다.** 링크만 넣고 "완료"라고 보고하면 대표님은 아무 변화도 못 보실 뻔했다.


### 5. 네이버 중복 5쌍 정리 — 지우면서 검색 경로를 잃지 않게

품질 점검에서 URL 중복 5건이 나왔다. 표기만 다른 같은 작품이 두 번 들어가 있었다(이번 투입과 무관한 기존 것).

- 마음의 소리 / 마음의소리
- 데드데이즈 / 데드데이즈(DEAD DAYS)
- 송곳 / 송곳 1~3부
- 하이브 / 하이브 1~2
- 심연의 하늘 / 심연의 하늘 시즌 1~3

다섯 쌍을 하나씩 열어 대조했다. 남기는 쪽은 전부 명작 큐레이션에 든 정식 표기였고, 순위 데이터에 물린 것도 없었다. **지우는 쪽 제목은 별칭(aliases)으로 옮겨 검색 경로를 살렸다.**

라이브에서 확인했다 — '마음의소리'를 붙여 써도 찾히고, 없어진 제목인 '송곳 1~3부'로 검색해도 살아 있는 '송곳' 한 편이 네이버 작품 페이지 링크와 함께 뜬다.

실행 결과: 12,864 → **12,859편**, URL 중복 5건 → **0건**, 링크 13,003개(리디 268 → 412).


### 6. 삭제 경고에 대한 대응 — 되돌릴 수 있게 만들어 두었다

대표님이 28번 실행 시 Supabase의 destructive 경고를 보고 물으셨다. "괜찮습니다"로 끝내지 않고, **삭제 전 5행의 값을 그대로 떠서 복구용 SQL(29번)을 만들어 올렸다.** 확인용 select 문도 함께 안내했다.

실행·검증 후 대표님 요청으로 29번은 삭제했다. 내용은 커밋 `e054c58`에 남아 있어 꺼낼 수 있다.


### 7. 유물 테이블 제거 — 내가 만든 함정에 대표님이 걸렸다

대표님이 "23번은 24번 하기 전에 돌렸는데 왜 미실행이냐"고 물으셨다. **양쪽 다 맞았다.** 23번 파일의 drop 문에 내가 08-16에 주석을 달아 놨기 때문이다. Run 하면 위의 확인용 select만 돌고 테이블은 남았다.

"승인 후 아래 한 줄을 실행한다"라고 파일 안에만 적어 두고, **전달할 때 "주석을 직접 풀어야 한다"는 말을 하지 않은 것이 불찰이다.** 파일을 Run 하면 끝나는 것으로 받아들이시는 게 당연했다.

주석을 풀어 커밋했으나 그 다음 실행에서도 테이블이 남아 있었다 — 아이클라우드 동기화 이전 파일을 여신 것으로 보였다. 파일 동기화를 기다리지 않고 **한 줄만 직접 붙여 실행하도록 안내**해 해소했다.

제거 후 확인: `work_rankings` 404(테이블 없음) · 나머지 7개 테이블 전부 200 · 홈 3.0초 · JS 오류 0 · supabase 4xx·5xx 0건 · 순위 화면 4탭 60칸 정상. 순위는 DB가 아니라 `data/rankings.json`을 읽으므로 화면 영향이 없다는 판단이 실측으로 확인됐다.


### 8. 자동화 예약 시각 조정 — 고장이 아니라 구조였다

아침에 07:00·07:20 두 건이 돌지 않은 것을 발견해 보고했다. 확인해 보니 고장이 아니라 지연이었고, 뒤늦게 정상 실행됐다(11:55 · 12:22).

원인은 GitHub 무료 예약 실행의 구조다. 예약 시각은 보장이 아니고, **정각(0분)은 전 세계 작업이 몰려 밀린다.** 그날 지연은 4시간 55분이었다.

**07:37 · 07:57로 옮겼다.** 사유를 워크플로 주석에 남겼다.


### 9. 파일 정리 — .sql / .md 이중 사본

대표님이 "같은 파일이 md·sql 두 개라 헷갈린다"고 하셔서 29쌍 전부를 diff로 비교했다. **예외 없이 내용이 동일**했으므로 .sql 29개를 삭제하고 .md만 남겼다. 남은 .sql 15개(01~16번)는 짝이 없는 스키마·초기 세팅용이다.

**앞으로 SQL은 .md 하나만 만든다.** 대표님이 아이패드 md 편집앱으로 여시기 때문이다.

08-22에 만들어 두고 커밋되지 않았던 수집 스크립트 2개와 SQL 18개도 이번에 함께 올렸다. `supabase/.temp/`는 gitignore에 추가했다.


### 10. 커밋

| 커밋 | 내용 |
|---|---|
| `2a70d9b` | 레진·리디 투입 + 링크 보강·중복 정리 SQL + index.html 링크 노출 개선 + 예약시각 조정 + .sql 29개 제거 |
| `e054c58` | 28번 되돌리기용 SQL (삭제 전 값 백업) |
| `5e65d99` | 되돌리기용 SQL 제거 (검증 완료로 불필요) |
| `8c00e53` | 23번 정리 문장 주석 해제 |


### 11. 사용한 에이전트 구성

| 구분 | 사용 | 비고 |
|------|------|------|
| Subagent | **미사용** | 메인 세션 단독 |
| 예약·반복 작업 | GitHub Actions 3건 (인기 순위 07:37 · 연재작 동기화 07:57 · keepalive 12:17) | 08-27에 앞의 두 건 시각 변경 |
| 도구 | Bash(DB REST 조회·SQL 생성·git·gh), playwright(라이브 실측 20회 이상), Edit/Write, 텔레그램 reply | SQL 실행은 대표님 |

**이번 구간의 원칙 준수** — 문서를 믿지 않고 DB를 찍었다. 전송량은 추정하지 않고 압축 전후를 각각 실측했다. 링크를 넣기 전에 그 링크가 화면에 닿는지 코드로 확인했다. 삭제는 되돌릴 수 있게 만든 뒤 실행했다. 보고 전에 라이브를 열어 검색어를 직접 눌러 봤다.


### 12. 이번 구간의 교훈

1. **handover.md는 스냅샷이지 진실이 아니다.** 08-22 작업이 문서에 없었다. 보고 전에 DB·워크플로·리포를 직접 확인한다.
2. **전송량은 압축 후로 말한다.** 원본 5.08MB / 실전송 0.98MB는 판단을 정반대로 바꾼다.
3. **데이터를 넣기 전에 화면에 닿는 경로를 본다.** `links[0]`만 쓰는 코드를 못 봤으면 보이지 않는 개선을 완료 보고했을 것이다.
4. **안전장치를 걸어 놨다면 그 사실을 전달까지 해야 한다.** 주석 처리한 drop 문이 대표님을 한 번 헛돌게 했다.
5. **정각 크론은 피한다.** GitHub 예약 실행은 시각을 보장하지 않는다.


## 2026-08-28 — 아티클 3·6번 본문 집필 (6편 시리즈 공백 해소 · 투입 대기)

대표님이 텔레그램으로 "1 진행해줘"라고 지시하셨다. handover.md 4절 1번, 화면에 "준비 중"으로 뜨던 아티클 두 편의 본문 작성이다. 카탈로그가 12,859편이 된 마당에 읽을거리에 구멍이 둘 남아 있던 건이다.


### 1. 착수 전 — 문서 대신 실제를 먼저 봤다

08-27의 교훈을 그대로 적용했다. handover를 믿지 않고 DB를 직접 조회했다.

| 확인 대상 | 결과 |
|---|---|
| `articles.body_ko` | 1·2·4·5번은 810~933자, **3·6번은 0자** — 문서와 일치 |
| 화면 렌더 경로 | `adoptArticles()`가 `body_ko`를 `\n{2,}`로 쪼개 문단 배열로 만들고, `renderArticleReader()`가 각 조각을 `<p>`로 감싼다 |
| 정적 fallback | `index.html`의 `ARTICLES` 배열에 1·2·4·5번만 `full:true`. 3·6번은 자리표시자 |

**여기서 하나를 미리 잡았다.** 3·6번은 기획서상 '데이터 시각화가 주인공'인 편이다. 그런데 렌더러가 모든 조각을 `<p>`로 감싸므로, 그림 블록을 넣으면 `<p><div>` 가 되어 브라우저가 문단을 강제로 끊는다. **데이터를 만들기 전에 그 데이터가 화면에 닿는 경로를 먼저 본다**는 08-27의 원칙대로, 집필 전에 렌더러를 확인한 것이 맞았다.


### 2. 집필 — 새 수치는 하나도 만들지 않았다

두 편 모두 `docs/지호_집필가이드_경제학웹툰.md`의 5단 구조·훈계 금지·출처 병기 원칙을 지켰고, 수치는 `docs/경제학콘텐츠_기획.md` §4에 이미 검증해 둔 공개 통계만 썼다.

**3번 「웹툰 1편 결제하면 그 돈은 어디로 가나」** (수익배분 · 5분 · 산문 1,292자)

- 1,000원 → 플랫폼(35~40%) → CP → 작가로 갈라지는 가치사슬을 구조로 설명
- 선지급금 차감 구조를 넣어 "작품이 잘된다"와 "작가가 번다" 사이의 시차를 짚었다
- 평균 8,540만 원(2023)은 상위 편중 값이라는 단서를 달았다
- 맺음: 불법은 "400원도 600원도 아니고, 나눌 것 자체가 생기지 않는다" → 4편으로 연결

**6번 「숫자로 보는 한국 웹툰」** (데이터 · 4분 · 산문 978자)

- 숫자 카드 5장 — 2조 1,890억 · 4,465억 · 20.4% · 8,540만 원 · 32.8% (전부 2023년, 카드마다 캡션 1~2문장)
- 피해 추정액 추이 3,932억(2022) → 4,465억(2023), 약 13.6% 증가
- 뒤에 "숫자 다룰 때 조심할 것 셋"을 붙였다 — ①출처가 다른 8,427억(2021, 문체부)과 섞지 않는 이유 ②평균은 가운데가 아니다 ③불법 이용률 20.4%와 '피해액이 매출의 약 20%'는 서로 다른 것을 잰다


### 3. 이번 구간에서 내린 판단 3가지

1. **없는 숫자는 지어내지 않고 '없다'를 그렸다.** CP와 작가가 나누는 비율은 공개 통계가 없다. 그럴듯한 수치를 넣는 대신 그 구간을 빗금으로 칠하고 "이 안의 분배 비율은 공개되지 않는다"라고 적었다. 숫자가 없다는 사실 자체가 이 사슬의 정보다.
2. **편수를 본문에 박지 않았다.** 6번 맺음에 카탈로그 규모를 넣으면서 "12,859편"이 아니라 **"1만 편 이상"**으로 썼다. 작품이 늘 때마다 본문을 고치게 되면 낡은 숫자가 화면에 남는다.
3. **연도가 확실하지 않은 수치는 뺐다.** 기획서 §4의 '저작권침해 경험 작가 54.6%'는 조사 연도가 명시돼 있지 않았다. 카드 한 장이 아쉬웠지만 "모든 숫자에 출처와 연도" 원칙을 지켜 제외하고 5장으로 갔다.


### 4. 코드 변경 — 세 곳, 모두 이번 작업과 직접 연결된다

| 파일 | 변경 | 이유 |
|---|---|---|
| `supabase/30_article_bodies.md` | 신규. `update` 2건 + 확인 select | 본문 투입. 달러 인용(`$b3$`)을 써서 본문 안의 따옴표를 escape 없이 담았다 |
| `index.html` · 렌더러 | `body=a.body.map(...)` 한 줄 — `<`로 시작하는 조각은 `<p>`로 감싸지 않는다 | 1절에서 확인한 문단 강제 종료 문제 |
| `index.html` · 정적 fallback | 3·6번을 `full:true` + 본문 배열로 교체 | 1·2·4·5번과 같은 형태 유지. DB 연결 실패 시에도 본문이 보인다 |
| `index.html` · 출처 박스 | 공통 출처 줄에 "불법이용 이유 '비용 부담' 32.8%" 추가 | 6번이 이 수치를 쓰는데 박스에 근거가 없었다 |

그림 블록은 CSS를 새로 만들지 않고 인라인 스타일 + 기존 `:root` 변수(`--blue` `--line-soft` 등)로 짰다. **본문이 DB 안에서 자족하게 하려는 것** — 아티클을 고칠 때 앱 코드를 건드리지 않아도 된다.


### 5. 검증

| 항목 | 방법 | 결과 |
|---|---|---|
| JS 문법 | `node --check` | 통과 |
| 렌더링 | playwright 390px · 1280px | 자리표시자 문구 소멸 · 페이지 오류 0건 |
| 그림 블록 | `.a-body` 자식 수 카운트 | 10개 · 8개 — 문단 수와 정확히 일치(빈 `<p>`로 깨지지 않음) |
| 그리드 반응형 | `gridTemplateColumns` 실측 | 390px 2열 · 1280px 4열. 처음 148px로 잡았다가 데스크탑 4+1이 어색해 185px로 올렸더니 모바일이 1열이 되어, **모바일 2열을 우선해 160px로 확정** |

라이브 실측이 아닌 로컬 파일 기준이다. DB 투입 전이라 실사이트에서는 아직 확인할 수 없다.


### 6. 남은 것 (대표님 액션 2건)

1. **`supabase/30_article_bodies.md` 실행** — SQL Editor에서 Run. 멱등이라 여러 번 돌려도 안전하다. 맨 아래 확인 select가 6편 전부의 본문 길이를 찍는다. 0인 편이 없으면 성공.
2. **푸시 승인** — `index.html` 변경은 푸시하면 곧바로 fairtoon.com에 반영된다. **외부 공개라 승인 없이 보내지 않았다.** 08-31 마무리 시점까지 미승인이라 로컬에만 있다(커밋도 하지 않았다).

08-31에 DB를 다시 조회해 3·6번이 여전히 0자임을 확인했다. 위 2건은 그대로 대기 중이다.


### 7. 사용한 에이전트 구성

| 구분 | 사용 | 비고 |
|------|------|------|
| Subagent | **미사용** | 메인 세션 단독. 대표님 지침대로 요청 없이는 쓰지 않았다 |
| 예약·반복 작업 | 신규 없음 | 기존 GitHub Actions 3건 유지 |
| 도구 | Bash(DB REST 조회·문서 확인·python 패치·node --check), playwright(렌더링 검증·스크린샷 4종), 텔레그램 reply | SQL 실행·푸시는 대표님 |


### 8. 이번 구간의 교훈

1. **집필도 '화면에 닿는 경로'를 먼저 본다.** 글만 쓰고 넘겼으면 그림 블록이 문단째 깨진 채로 올라갔을 것이다.
2. **없는 숫자를 그리는 것도 시각화다.** 빈칸을 그럴듯한 수치로 메우는 순간 포트폴리오의 신뢰가 깎인다.
3. **본문에 시시각각 변하는 숫자를 박지 않는다.** 낡을 숫자는 "1만 편 이상"처럼 범위로 쓴다.
4. **반응형은 한쪽을 고르는 일이다.** 148 → 185 → 160으로 두 번 되돌린 끝에 "모바일 2열 우선"이라는 기준을 정하고 확정했다.


## 2026-08-31 — 30번 SQL 실행 · 아티클 3·6번 라이브 반영 (6편 시리즈 완성)

대표님이 텔레그램으로 "30번 쿼리 실행했어. 푸쉬해"라고 지시하셨다. 08-28에 집필만 끝내고 대기하던 두 건(SQL 실행 · 푸시 승인)이 한 번에 풀렸다.


### 1. 푸시 전에 DB부터 찍었다

"handover를 진실로 믿지 말 것"이라는 08-27의 교훈대로, 실행됐다는 말을 그대로 받지 않고 REST로 `articles`를 직접 조회했다.

| 편 | slug | 본문 길이 | 문단 수 | 그림 블록 |
|---|---|---|---|---|
| 3번 | `revenue-share-value-chain` | 2,926자 | 10 | 2개 (4번·9번 조각, 줄바꿈 없이 온전) |
| 6번 | `korea-webtoon-by-numbers` | 3,521자 | 8 | 1개 (2번 조각, 2,529자) |

1·2·4·5번도 810~933자로 그대로였다. **`body_ko`가 0자인 편은 없다.** 문단 수는 `index.html`의 정적 본문 배열(10개·8개)과 정확히 일치했다 — 빈 줄이 뭉개지지 않았다는 뜻이다.


### 2. 커밋·푸시

`ac5d159` → 리베이스 후 `67f5fdf`. 원격에 08-31 인기 순위 자동 커밋이 있어 `git rebase --autostash origin/main`으로 얹었다(늘 걸리는 자리다).

- `index.html` — 3·6번 정적 본문(DB 미응답 대비 fallback) + `full:true` 전환, 렌더러 한 줄(`<`로 시작하는 조각은 `<p>`로 감싸지 않는다), 출처 박스에 '비용 부담 32.8%' 추가
- `supabase/30_article_bodies.md` — 실행본을 리포에 보존
- `docs/경제학콘텐츠_기획.md` · `docs/지호_집필가이드_경제학웹툰.md` — handover 6절이 가리키던 문서인데 리포에 없었다. 함께 올렸다

**이번 푸시에서 제외한 것**: `Renew/` · `project-notes/` · `etc/`(통화 녹취 m4a·스크린샷) · `output/` 시안 폴더들 · `sian/` 신규 이미지. 디자인 리뉴얼 쪽 작업물이고, 특히 통화 녹취는 공개 리포에 올릴 성격이 아니라 대표님 판단을 받기로 했다.


### 3. 라이브 검증 — 말이 아니라 화면으로

푸시 직후 fairtoon.com이 곧바로 새 파일을 냈다(폴링 0회). 헤드리스로 390px·1280px 두 폭에서 두 편을 실제로 열었다.

| 확인 항목 | 3번 | 6번 |
|---|---|---|
| `<p>` 안에 `<div>` 중첩 | **0건** | **0건** |
| 자리표시자 문구 잔존 | 없음 | 없음 |
| 가로 스크롤 넘침 | 없음 | 없음 |
| JS 오류 | 0 | 0 |

아티클 목록 화면에서도 "준비 중"·"자리표시자"·"샘플 기사" 문구가 **0회**로 사라졌다. 스크린샷으로 그림도 눈으로 봤다 — 3번의 1,000원 배분 막대(빗금 구간 포함)와 6번의 숫자 카드 5장이 모바일에서 의도한 대로 2열로 접힌다.


### 4. 사용한 에이전트 구성

| 구분 | 사용 | 비고 |
|------|------|------|
| Subagent | **미사용** | 메인 세션 단독 |
| 예약·반복 작업 | 신규 없음 | 기존 GitHub Actions 3건 유지 |
| 도구 | Bash(REST 조회·git·라이브 폴링), playwright(390/1280px 렌더 검증·스크린샷), 텔레그램 reply | — |


### 5. 남은 것

PM 쪽 대기 항목이 하나도 없다. 남은 것은 전부 대표님 결정이다 — **제휴 문의 발송(명의·회신 이메일)** · '지금 인기' 상단 메뉴 편입 · 지호님 작가명 검수 7건 · 리뉴얼 작업물 리포 반영 여부.


## 2026-08-31 (오후) — 리뉴얼 작업물 리포 반영 · 작가명 7건 대조 · '지금 인기' 판단

대표님 지시 네 건을 처리했다. "제휴 문의는 보류하고 우선순위 낮게, 리뉴얼 작업물은 리포에 반영, '지금 인기' 편입 의견, 작가명 검수 방법 상세 안내."


### 1. 제휴 문의 — 보류로 내려놓았다

`handover.md` 3절에서 빼고 4절 맨 앞에 **0번(보류·우선순위 낮음)**으로 옮겼다. 문안·재개 시 손볼 두 가지(편수 12,859편 갱신 · "고지" 문단 유지)는 그대로 적어 두었고, **"먼저 여쭙지 말 것"**을 명시했다. 대표님이 꺼내실 때까지 PM이 다시 올리지 않는다.


### 2. 리뉴얼 작업물 117개 반영 (`25dba7a`)

`Renew/`(영문 5 + `kr/` 5) · `project-notes/`(영문 11 + `kr/` 11) · `output/디자인시안`·`로고시안`·`표지목업`·`가격비교 팩트체크` · `sian/` 7장 · `imgs/fairtoon_logo.png` · `etc/`(스포티파이 디자인 노트 · 프리뷰7 · 스크린샷) · `docs/디자인_레퍼런스.md`·`docs/유스임팩트_통화_아젠다_2026-06-30.md`.

**올리기 전에 확인한 것**

| 확인 | 결과 |
|---|---|
| 리포 공개 여부 | `gh repo view` → **PUBLIC**. 커밋 = 공개다. 직전 보고에서 이 사실을 알린 뒤 받은 지시라 그대로 진행했다 |
| 인증정보·개인정보 스캔 | 전화번호·메일·토큰·계좌 패턴 0건. `project-notes/03_조직.md`도 부서 단위 기술이고 실명이 없다 |
| 대용량 파일 | 20MB 초과 0건 (최대 `sian/e.png` 6.9MB) |
| 사이트 영향 | 푸시 후 홈 200 · `/admin/` 200 정상 |

**일부러 뺀 것 두 가지**

1. **유스임팩트 통화 녹취 m4a** — 공개 리포에 통화 녹음을 올리는 것은 성격이 다르다고 판단해 제외하고, `.gitignore`에 `etc/*.m4a`를 넣어 다음에 실수로 올라가지 않게 막았다. 올리라고 하시면 그 한 줄만 지우면 된다.
2. **아이클라우드가 만든 `" 2.확장자"` 사본 33개** — 스테이징에 딸려 들어온 것을 발견해 md5로 전수 비교했더니 **33개 전부 원본과 해시가 같았다.** 영구 이력에 남는 중복이라 스테이징에서 뺐다. **디스크에서 지우지는 않았다** — 파일 삭제는 대표님 몫이다.


### 3. 작가명 7건 — '방법 안내'를 '승인만 하면 되는 상태'로 바꿨다

대표님이 "검수 방법을 자세히 알려달라"고 하셨는데, 방법만 적어 보내면 지호님이 7편을 일일이 찾아보셔야 한다. **근거를 PM이 먼저 다 모아 두고, 판단만 남기는 편이 낫다고 보고 그렇게 했다.**

플랫폼 공식 출처로 7건 전부 대조했다.

| 작품 | DB 값 | 플랫폼 실제 표기 | 판정 |
|---|---|---|---|
| 신도림 | 전선욱 | **오세형** (글·그림 겸) | DB 오류 |
| 랜덤채팅의 그녀! | 마사토끼·레노 | **박은혁** (글·그림 겸) | DB 오류 |
| 어글리후드 | 나야 | **미애** (글·그림 겸) | DB 오류 |
| 아 지갑놓고 나왔다 | 치즈 | **미역의효능** | DB 오류 |
| 악녀는 마리오네트 | 한이레·만보 | **정인·망글이·한이림** | DB 오류 |
| 김비서가 왜 그럴까 | 정경윤 | **김명미·정경윤** | 원작만 있었다 → 작화 추가 |
| 트레이스 | 고영훈 | **네스티캣** | 본명/필명 → 정책 결정 |

**수확이 하나 있었다 — 네이버는 역할 코드를 준다.** 작품정보 API의 `communityArtists[].artistTypeList`가 `ARTIST_WRITER`(글)·`ARTIST_PAINTER`(그림)를 구분한다. **네이버 3,120편은 앞으로 자동 대조가 가능하다.** 카카오는 이름만 나열하므로(`김명미,정경윤`) 불가하다. 카카오 3편과 카카오웹툰 1편은 헤드리스 브라우저로 작품 페이지 크레딧 줄을 직접 읽었다(bff graphql은 403).

또 하나 — **`works` 테이블에 `art_author` 열이 이미 있는데 49편 전부 비어 있다.** 지금은 `author` 한 칸에 `·`로 몰아넣고 있다. 글/그림을 나눠 쓸지 대표님·지호님 결정을 받기로 하고 가이드에 선택지로 넣었다.

산출물: **`output/작가명_검수가이드.md`** — 근거 우선순위, 네이버 역할 코드 설명, 7건 대조표, 결정 필요 2건(필명 표기 · 글/그림 분리), 답하는 방법 3가지, 확인용 링크 7개.


### 4. '지금 인기' 상단 메뉴 편입 — PM 의견

**넣는 쪽이 맞다고 보고, 이름은 '오늘의 순위'로 바꿔 넣기를 권했다.**

- 찬성 근거 — ①매일 갱신되는 유일한 코너이자 재방문 이유인데 홈 더보기 뒤에 숨어 있다 ②"요즘 뭐가 인기인지 한눈에"는 불법 사이트를 찾는 이유와 정면으로 겹치는 지점이라, 합법 링크와 함께 주는 것이 4·5번 아티클의 '마찰 줄이기'를 화면으로 실행하는 셈이다 ③비용 10분
- 반대 근거와 대응 — 메뉴 7개는 실질 문제이나 '작가의 목소리'가 아직 플레이스홀더라 교대 가능 / 우리 데이터가 아닌 점은 통합하지 않고 플랫폼별로 나눠 두는 현 설계로 대응 / **갱신이 멈추면 낡은 순위가 대문에 걸리는 위험은 실재** → `data/rankings.json` 갱신 시각이 48시간을 넘으면 "갱신 지연"을 표시하는 안전장치를 함께 넣기를 제안
- 이름 — '지금 인기'는 실시간처럼 들리는데 실제로는 하루 한 번(07:37)이다. **화면이 약속하는 것과 실제가 어긋나면 신뢰가 깎인다.** 숫자를 다루는 이 사이트의 태도와도 어긋난다.

전문은 `handover.md` 7절에 기록했다.


### 5. 사용한 에이전트 구성

| 구분 | 사용 | 비고 |
|------|------|------|
| Subagent | **미사용** | 메인 세션 단독 |
| 예약·반복 작업 | 신규 없음 | — |
| 도구 | Bash(gh repo view·git·해시 비교·네이버 API), playwright(카카오 3사 크레딧 확인), Write/Edit, 텔레그램 reply | — |


### 6. 이번 구간의 교훈

1. **"방법을 알려달라"는 요청은 대개 "이 일을 줄여 달라"는 뜻이다.** 절차만 적어 보내는 대신 근거를 먼저 모아 두니, 지호님 작업이 조사에서 승인으로 바뀌었다.
2. **공개 리포에 올리는 것은 게시다.** 올리기 전에 공개 여부를 `gh`로 확인하고, 인증정보·개인정보를 스캔하고, 성격이 다른 파일(통화 녹취)은 빼고 재발 방지선(.gitignore)까지 그었다.
3. **딸려 들어온 파일을 그냥 커밋하지 않는다.** 아이클라우드 중복 33개는 해시로 확인하고 뺐다. git 이력은 지워지지 않는다.
4. **자동화 가능성은 검수하다가 발견된다.** 네이버 역할 코드는 7건을 확인하러 갔다가 얻은 것이고, 3,120편에 쓸 수 있다.


## 2026-08-31 (저녁) — 31번 실행 완료 · 작가 표기에 그림 작가가 붙었다

대표님이 "1~6 승인, 7번 A, 원작/작화 A"로 답을 주셨다. SQL 작성 → 실행 → 검증까지 마쳤다. **다만 실행까지 두 번 실패했고, 둘 다 PM 잘못이다.**


### 1. 승인 범위를 넘어서 49편을 전수 대조했다

승인받은 것은 7건이지만, 같은 기준을 49편 전체에 적용했다.

| 플랫폼 | 편수 | 일치 | 불일치 |
|---|---|---|---|
| 네이버 | 28 | 25 | 3 (전부 승인분) |
| 카카오웹툰 | 14 | 11 | 3 (트레이스 · 은밀하게 위대하게 · 나빌레라) |
| 카카오페이지 | 7 | 0 | 7 (승인분 3 + **신규 4**) |

**신규 4건** — 황제의 외동딸 작화가가 '로담'이 아니라 **리노** · 사내맞선 '들깨·NARAK' 누락 · 템빨 '이동욱(REDICE STUDIO)·Team Argo' 누락 · 나혼렙 각색 '현군' 누락. 승인 목록 밖이라 **32번으로 분리**해 대표님 판단을 받기로 했다.


### 2. 방법론 — 카카오페이지의 역할을 알아내는 법

카카오페이지는 이름만 나열하고 역할을 밝히지 않는다(`정인,망글이,한이림`). **같은 플랫폼에서 웹소설판을 찾으면 원작자가 특정된다.**

- 김비서: 웹소설 `정경윤` 단독 → 정경윤 원작, 김명미 작화 ✔ 확인
- 황제의 외동딸: 웹소설 `윤슬` → 윤슬 원작, 리노 작화 ✔ 확인

**2인 크레딧은 이 방법으로 나눌 수 있고, 3인 크레딧은 나눌 수 없다.** 원작자는 특정되지만 나머지 둘 중 누가 작화이고 누가 각색인지는 어디에도 없다. 그래서 사내맞선·템빨·나혼렙은 한 칸에 그대로 두었다. **추측으로 나누면 창작자 크레딧을 우리가 임의로 고치는 셈이다.**

네이버는 역할 코드(`ARTIST_WRITER`/`ARTIST_PAINTER`)를, 카카오웹툰은 작품정보 탭의 '글'·'그림'을 준다. **이 둘은 앞으로 자동 대조가 된다.**


### 3. 반영 결과 (31번 · 15편)

| 구분 | 내용 |
|---|---|
| 작가명 교체 6 | 신도림 오세형 · 랜덤채팅 박은혁 · 어글리후드 미애 · 아 지갑놓고 미역의효능 · 악녀 정인·망글이·한이림 · 은밀하게 Hun |
| 필명 정책(A) | 트레이스 표시는 **네스티캣**, 본명 '고영훈'은 `aliases`로 — 라이브에서 '고영훈' 검색 시 트레이스가 뜬다 ✔ |
| 글/그림 분리 8 | 노블레스 · 스위트홈 · 지옥 · 죽음에 관하여 · 놓지마 정신줄 · 심연의 하늘 · 나빌레라 · 김비서 |

DB 재조회 결과 **15/15 전부 기대값과 일치**, `art_author` 채워진 편수 8. 라이브 검증도 마쳤다 — 명작 카드에 "글 손제호 · 그림 이광수"가 뜨고, 그림 작가 이름('이광수')으로도 검색된다. JS 오류 0.


### 4. 화면 수정 (`6ff7dcd`)

`authorText(c,long)` 헬퍼 하나로 통일했다. 넓은 카드는 "글 ○○ · 그림 ○○", 좁은 카드(찾아보기·순위)는 자리가 없어 "○○·○○". `loadContent` select에 `art_author` 추가, 검색 haystack에도 포함. 정적 fallback의 트레이스·노블레스도 맞췄다.


### 5. 두 번 실패했다 — 둘 다 PM 잘못

| 실패 | 원인 | 조치 |
|---|---|---|
| `syntax error at or near "```"` | SQL `.md`를 ```` ```sql ```` 코드펜스로 감쌌다. **기존 27·28·30번은 전부 순수 SQL이었는데 이번만 형식을 어겼다** | 펜스 제거, 뒤의 마크다운 설명도 `--` 주석으로 전환 |
| `malformed array literal: "고영훈"` | `text[] \|\| '문자열'`을 Postgres가 배열 리터럴로 읽는다 | `array_append(arr, '고영훈'::text)` |

**두 번째 실패 직후 가장 먼저 한 일은 DB 재조회였다.** 앞선 update 6건이 반쯤 들어갔는지 확인해야 했기 때문이다. **Run 한 번은 통째로 롤백된다**는 것을 실측으로 확인했고(15편 전부 원래 값), 대표님께 "중복 실행 걱정 없이 그대로 다시 돌리시라"고 안내할 수 있었다.

세 번째 시도에서 성공. **이 맥에는 SQL을 파싱해 볼 Postgres가 없다**(psql·pglast 설치 실패). 눈으로만 검토한 것이 원인이므로, 앞으로 배열·타입 관련 표현은 모호성이 없는 함수 형태로만 쓴다.


### 6. 사용한 에이전트 구성

| 구분 | 사용 | 비고 |
|------|------|------|
| Subagent | **미사용** | 메인 세션 단독 |
| 도구 | Bash(네이버 작품정보 API 28회 · REST 검증 · git), playwright(카카오웹툰 14편 역할 · 카카오페이지 웹소설 대조 · 라이브 확인·캡처), Write/Edit, 텔레그램 reply | SQL 실행은 대표님 |


### 7. 이번 구간의 교훈

1. **형식을 바꿀 때는 기존 파일을 먼저 본다.** 27·28·30번을 열어 봤으면 코드펜스를 붙이지 않았다.
2. **검증할 수 없는 문법은 쓰지 않는다.** 로컬에 파서가 없으면 `array_append`처럼 모호성 없는 형태를 고른다.
3. **실패했을 때 첫 행동은 상태 확인이다.** "얼마나 들어갔는가"를 먼저 재고 안내해야 대표님이 중복 실행을 걱정하지 않는다.
4. **모르는 것을 나누지 않는다.** 3인 크레딧을 추측으로 글/그림에 배분했으면 창작자 정보를 우리가 왜곡한 것이 된다.


## 2026-09-02 — '지금 인기' 상단 메뉴 편입 (다섯 번 물었던 항목)

대표님이 "'지금 인기' 상단 메뉴 편입은 내가 진행하자고 하지 않았었나?"라고 물으셨다.


### 1. 기록을 먼저 찾아봤다

backup의 handover 스냅샷 6개를 훑었다. 08-16(두 번)·08-17(세 번)·08-19·08-21·08-28(네 번)·08-31(다섯 번) — **전부 '대표님 결정 대기'로만 남아 있고 진행 지시는 기록에 없다.** 08-31에도 대표님 지시는 "장단점에 대해 클상무는 어떻게 생각해?"였다.

**그래서 기록상으로는 승인이 없었다고 답하되, 따지지 않고 바로 넣었다.** PM이 다섯 번이나 권했던 10분 작업을 두고 승인 여부를 다투는 것은 의미가 없다. 다섯 번 물어서 답을 못 받았다는 것은 **묻는 방식이 잘못됐다는 신호**이기도 하다 — 처음부터 의견과 함께 물었어야 했다.


### 2. 반영 내용 (`3b91587`)

- **상단 메뉴에 '지금 인기' 추가** — 데스크톱·모바일 양쪽, 명작 큐레이션 다음 자리. 푸터 SITE 목록에도 넣었다
- **i18n `nav_popular`** (ko 지금 인기 / en Popular Now)
- **`goPopular()`가 해당 메뉴를 활성으로 표시** — 예전에는 홈 더보기로 들어오면 활성 메뉴가 아무것도 없어 위치를 알 수 없었다
- **`rankStaleDays()` 안전장치** — `fetched_on`이 이틀 넘게 멈추면 '지금 인기' 화면 머리말 아래에 "갱신이 N일째 밀려 있습니다. 아래 순위는 YYYY-MM-DD 기준이며…"를 띄운다

**이름은 기존 '지금 인기'를 그대로 썼다.** 대표님이 그 이름으로 말씀하셨기 때문이다. PM 권고('오늘의 순위')는 handover 3절에 한 줄 작업으로 남겼다.


### 3. 안전장치를 넣은 이유

이 코너는 이제 대문에 걸린다. **08-27에 자동화가 4시간 55분 밀린 전례가 있다.** 상단 메뉴에 걸린 코너가 낡은 순위를 조용히 보여주면, 숫자에 출처와 기준일을 붙이는 이 사이트의 태도와 정면으로 어긋난다.

임계값은 **2일**로 잡았다. 갱신은 07:37이므로 자정~07:37 사이에는 정상 상태에서도 `fetched_on`이 어제 날짜다(=1일). 2일이면 하루치 실행을 통째로 놓친 것이다.


### 4. 검증

| 항목 | 결과 |
|---|---|
| 상단 메뉴 넘침 | 1440·1280·1024px 전부 없음 (nav 601px) · 모바일은 버거 안 세로 목록 |
| 메뉴 클릭 | `#/popular`로 이동, '지금 인기'가 활성 표시 |
| 모바일 버거 | 8개 항목 정상 노출 |
| 지연 표시 · 정상 상태 | `fetched_on=2026-09-01`, 1일 → **표시되지 않음** (의도대로) |
| 지연 표시 · 지연 상태 | 08-30으로 바꿔 재렌더 → "갱신이 3일째 밀려 있습니다…" 정상 표시 |
| JS 오류 | 라이브 0 |

라이브 반영 확인 완료.


### 5. 사용한 에이전트 구성

| 구분 | 사용 | 비고 |
|------|------|------|
| Subagent | **미사용** | 메인 세션 단독 |
| 도구 | Bash(backup 스냅샷 grep · git · 로컬 http.server), playwright(4개 폭 nav 검증 · 지연 표시 시뮬레이션 · 라이브 확인) | — |


### 6. 이번 구간의 교훈

**결정을 물을 때는 처음부터 의견을 붙여 묻는다.** 같은 질문을 다섯 번 반복하는 동안 PM 의견은 한 번도 붙지 않았다. 08-31에 의견을 적어 드리자 바로 결론이 났다. **"넣을까요?"가 아니라 "넣는 게 맞다고 봅니다, 이유는 이것이고 반대 논거는 이것입니다"로 물어야 답이 온다.**
