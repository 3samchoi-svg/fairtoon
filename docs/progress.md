# Fairtoon 프로젝트 진행 기록 (progress.md)


날짜별 누적 히스토리. "무엇을 했는가"의 기록. 현재 스냅샷·다음 액션은 `handover.md` 참조.

> **이전 이력 아카이브**
> - `backup/progress_2026-08-16-1442.md` (2026-08-02 ~ 08-15, 335줄) — P4 본격 진행. 카탈로그 50편 → 3,017편, 인기 순위·작품 찾아보기 화면 신설.
> - `backup/progress_2026-08-03-0053.md` (2026-06-17 ~ 08-02, 391줄) — 착수부터 P0~P3 완료·P4 착수까지.


## 이어지는 맥락 (carry-over · 2026-08-16 기준)

- **카탈로그 4,504편** (완결 2,430 · 연재 2,074). 표지 4,503장. 명작 큐레이션은 여전히 49편.
- **네이버 완결작 수집 종료.** 5차(page 59~69)에서 목록 끝에 도달했다. 더 훑을 페이지가 없다.
- **연재작 동기화는 로직 검증까지 끝났고 자동화 배포만 남았다.** 함수·워크플로 모두 작성·등록 완료. 대표님이 Supabase CLI로 배포 + GitHub Secret 등록을 하면 매일 07:20에 돈다.
- **대기 — 카카오웹툰 완결 전환은 자동화에서 제외했다.** 상태를 API로 주지 않아 브라우저 렌더가 필요하다. PM이 월 1회 수동 처리한다.
- **대기 — 제휴 문의 발송**: 문안 완성, 발송 명의·회신 이메일 미정.
- **대기 — 지호님 작가명 검수 7건**, 아티클 3·6번 본문.


## 2026-08-16 — PM 실행 대기 8건 일괄 처리 · 카탈로그 4,504편


### 1. 작가의 목소리 — 두 번 눌러야 재생되던 것 (대표님 지적)

대표님 지적: 영상을 누르면 상세 페이지로 한 단계 들어간 뒤 다시 재생 버튼을 눌러야 했다.

카드를 누르면 **그 자리에서 큰 플레이어가 뜨고 곧바로 재생**되도록 바꿨다(라이트박스). 홈 선반·목록 페이지 양쪽 적용. 닫기는 X·바깥 클릭·ESC 세 가지이고, 닫을 때 iframe을 비워 재생을 끊는다.

카드가 3:4 포스터 모양이라 카드 안에서 재생하면 화면이 너무 좁다. 그래서 인라인 재생 대신 라이트박스를 택했다. 인용문·숏츠·요약이 있는 상세 페이지는 '자세히 보기 →'로 유지했다.


### 2. 카카오웹툰 링크가 검색 페이지로 가던 것 59편 (대표님 지적)

대표님이 '무장 - 무투전'을 예로 들어 지적. 전수 조사 결과 **카카오웹툰 198편 중 59편이 검색 URL**이었고, 네이버 2,776편·카카오페이지 43편에는 한 건도 없었다. 문제는 카카오웹툰에만 있었다.

원인은 작품 페이지가 외부 요청에 403이라 수집 당시 검색 주소로 낮춰 둔 것이 그대로 남은 것이다. **카카오웹툰 자체 검색 API(`gateway-kw.kakao.com/search/v2/content`)가 contentId·seoId를 그대로 준다**는 것을 찾아 59편 전부 복구했다. 제목·작가명 완전일치로 확인하고 무작위 6편은 실제 브라우저로 열어 검증했다.

**부수 성과**: 표지가 없던 60편 중 59편이 정확히 이 작품들이었다. 링크가 막혀 표지도 못 가져왔던 것이다. og:image(`/sharing/2x/` · 600×600)로 복구해 **표지 누락 60편 → 1편**이 됐다. 기존 카카오웹툰 138편이 이미 쓰던 형식과 같아 카드 모양이 어긋나지 않는다.


### 3. 연재작 동기화 1차 — 카카오페이지 1,153편 누락 발견

handover의 미해결 항목 "카카오페이지 연재작이 36편뿐인 정황"의 원인을 찾았다. **요일연재 목록 API가 페이지 단위로 끊기는데 기획 당시 첫 페이지만 읽어 플랫폼 전체를 126편으로 잡았다.** 7개 요일 탭을 끝까지 넘겨 보니 웹툰 1,494편이고, DB에 없는 비성인 작품이 1,153편이었다.

**PM의 실수다.** 있는 줄 알았던 플랫폼의 96%가 없었다. 표지는 1,153편 전부 HEAD 요청으로 확인해 누락 0건.

**완결 전환에서 함정을 피했다.** "요일 목록에서 사라졌으면 완결"로 처리하면 30편을 잘못 바꾼다. 요일 편성 없이 상시 연재하는 작품이 그만큼 있다. 후보 36편을 작품별로 원본에서 확인해 실제 완결 6편만 골라냈다.

- 네이버 — 작품정보 API의 `finished` 플래그
- 카카오웹툰 — 회차 목록 최신 항목의 `(완결)` 표기. **요일 목록·profile API에는 상태 필드가 없다.**

**같은 조사에서 카카오페이지 완결 탭(`tab_uid=12`)도 찾았다.** handover의 또 다른 미해결 항목이 함께 풀렸다.


### 4. 완결작 5차 331편 — 네이버 완결 목록 끝까지 도달

page 59에서 시작해 **page 70이 비어 목록이 끝났다.** 331편 수집, 성인물 21편·중복 129편 제외. 중복 기준(250편)에 닿지 않아 목록 전체 재훑기 전환은 불필요했다. 딥링크 표본 30건 200, **표지 331편 전수 200**.

9회차 계획이었으나 5회차에서 종료. 네이버 완결작 수집은 이것으로 끝이다.


### 5. 플랫폼 순위 탭 링크가 다른 플랫폼으로 새던 것 14건

`hcardHTML`이 탭의 플랫폼이 아니라 그 작품의 DB 링크를 썼다. 카카오 양 서비스가 같은 작품을 함께 서비스하므로 오늘 기준 **양방향으로 14건**이 어긋났다(카카오페이지 탭 → 카카오웹툰 11건, 반대 3건). 함수에 플랫폼을 넘겨 탭과 일치시켰다. 그 플랫폼의 딥링크는 없으므로 검색으로 보낸다. 3개 탭 90장 전부 도메인 일치 확인.


### 6. 검색 결과 화면 — 카탈로그가 커지며 드러난 문제들

- '나'로 검색하면 **273편이 한 번에 그려졌다** → 24편씩 '더 보기'
- 정렬이 없어 '미생'을 찾으면 '미생물'이 먼저 나올 수 있었다 → 제목 완전일치 → 시작일치 → 포함 → 작가에서만 걸린 것 순
- 결과 카드가 딥링크를 무시하고 플랫폼 검색으로만 갔다 → 작품 페이지로 직행
- 문구가 "큐레이션에서 N편"이었다 → 전체 카탈로그를 검색하므로 "작품 정보에서 N편"으로 정정

**명작 큐레이션 49편의 '○○에서 보기 ↗'도 같은 이유로 딥링크를 흘리고 있었다.** 한 줄 문제라 함께 고쳤다.


### 7. 파비콘·앱아이콘·공유이미지 — 확정 로고 반영

미착수 사유였던 "로고에서 마크만 뗄 수 없다"를 실제로 확인했다. **사실이었다** — 책 하단이 워드마크 배너에 가려 크롭하면 잘린 책이 된다.

그래서 **펼친 책을 SVG로 다시 그렸다.** 16px 탭에서도 읽힌다. 앱아이콘(180px)·공유이미지(1200×630)도 교체했다. 기존 것은 옛 임시 로고(말풍선+체크)를 쓰고 있었다. 공유이미지 생성기는 `etc/og-image-source.html`에 남겼다.


### 8. 동기화 2차 — 로직 검증 통과

1차 SQL이 DB에 반영된 뒤 같은 로직을 다시 돌렸다.

- **신규 0편** — 1차가 빠짐없이 반영됐고 두 번 돌려도 중복이 생기지 않는다
- **완결 후보 30편 → 실제 전환 0편** — 요일 목록에 없지만 연재 중인 30편을 건드리지 않았다

A안(수동 검증)의 목적을 달성했다. D안(자동화)으로 진행 가능.


### 9. 동기화 자동화 — D안 구현

전권 키를 GitHub에 두지 않는 구조를 그대로 구현했다. 전권 키는 Supabase 안에만 있고, Actions는 함수 하나를 부를 토큰(`SYNC_TOKEN`)만 갖는다.

- `supabase/functions/sync-serial/index.ts` — 동기화 함수. `dry=1`이면 쓰지 않고 결과만 돌려준다
- `supabase/functions/README.md` — 배포 절차·토큰 설정·문제 대응표
- `.github/workflows/sync-serial.yml` — 매일 07:20 KST (순위 갱신 20분 뒤)

**카카오웹툰 완결 전환은 자동화에서 제외했다.** 상태를 API로 주지 않아 브라우저 렌더가 필요하고, 그건 Edge Function 런타임에서 못 한다. 함수는 후보 제목만 응답에 담고 건드리지 않는다. **확인할 수 없는 것을 추측으로 바꾸면 카탈로그에 오류가 조용히 쌓인다.**


### 10. PAT workshop scope — 몇 주 묵은 항목이 해소됐다

워크플로 파일 푸시가 거부됐다. 원인은 리포 토큰에 `workflow` 권한이 없는 것. **08-03 carry-over에 "휴면 방지 방식 선택(PAT workflow scope 부재로 push 거부됨)"으로 적혀 있던 그 문제다.**

대표님이 토큰을 갱신해 주셔서 `sync-serial.yml`과 **몇 주째 로컬에만 있던 `keepalive.yml`**을 함께 등록했다. Actions에 3개 워크플로가 active로 확인된다.


### 11. SQL 파일 확장자 — 원인 확인 및 대응

handover에 "supabase md 사본이 삭제 상태로 잡힌다"고 남겨 둔 현상의 원인은 **대표님이 아이패드 md 편집앱으로 열기 위해 `.sql` → `.md`로 바꾸신 것**이었다. 앞으로 PM이 SQL을 만들 때 `.md` 사본을 함께 만들어 리포에 넣는다. 17~23번은 소급 적용했다.


### 12. 반영 결과 검증

대표님이 18~22번 SQL을 실행한 뒤 전수 확인했다.

| 항목 | 결과 |
|------|------|
| 전체 작품 | 3,017 → **4,504편** (기대값 일치) |
| 상태 | 완결 2,430 · 연재 2,074 |
| 링크 | 4,504개 (네이버 3,110 · 카카오페이지 1,196 · 카카오웹툰 198) · 고아 0 |
| 표지 없음 | 60 → **1편** ('황녀 스베타의 정략결혼') |
| 카카오웹툰 검색URL | 59 → **0건** |
| 인기 순위 빈자리 | 27 → **1편** ('아비무쌍') |
| 명작 큐레이션 | 49편 유지 |

**사이트가 4,504편 전량을 받는지 세어서 대조했다**(1,000행 페이징 5페이지). 라이브 자산 3개는 fairtoon.com에서 직접 받아 해시로 대조했다.


### 13. 사용한 에이전트 구성

| 구분 | 사용 | 비고 |
|------|------|------|
| Subagent | **미사용** | 전 작업 메인 세션 단독 처리 |
| 예약·반복 작업 | GitHub Actions **3건** | 인기 순위(기존) + 연재작 동기화·keepalive(신규 등록) |
| 백그라운드 실행 | 2건 | 완결작 5차 수집, 동기화 2차 — 속도 제한 준수로 장시간 소요 |
| 도구 | Playwright(헤드리스 검증·완결 판정), Python 수집 스크립트, esbuild(TS 검사), gh, git | 화면은 전부 실제 렌더로, 편수는 전부 세어서 대조 |


### 14. 이번 구간의 교훈

- **목록 API는 첫 페이지만 읽으면 안 된다.** 카카오페이지 1,153편 누락의 원인이 이것 하나였다. 페이지네이션을 끝까지 넘기고, 얻은 편수를 플랫폼 공표 규모와 대조해야 했다.
- **"목록에서 사라짐"은 완결의 근거가 아니다.** 상시 연재작이 존재한다. 상태는 작품별 원본으로 확인한다.
- **판정할 수 없으면 건드리지 않는다.** 카카오웹툰 완결을 추측으로 바꾸지 않고 수동 처리로 남긴 판단.
- **막힌 항목의 사유를 다시 검증할 가치가 있다.** '로고 마크 분리 불가'는 사실이었지만 다시 그리면 됐고, 'keepalive 미배포'는 권한 한 줄 문제였다. 둘 다 몇 주간 대기 상태였다.
